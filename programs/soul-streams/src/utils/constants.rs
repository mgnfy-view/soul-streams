pub mod seeds {
    pub const STREAM_COUNT: &[u8] = b"stream_count";
    pub const STREAM: &[u8] = b"stream";
    pub const TOKEN_ACCOUNT: &[u8] = b"token_account";
}

pub mod general {
    pub const DISCRIMINATOR_SPACE: usize = 8;
}
