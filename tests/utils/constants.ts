import { SeedStrings } from "./types";

const seedStrings: SeedStrings = {
    streamCount: "stream_count",
    stream: "stream",
    tokenAccount: "token_account",
};

const DECIMALS = 9;

export { seedStrings, DECIMALS };
